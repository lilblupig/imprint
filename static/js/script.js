$(document).ready(function(){
    /* Materialize - Display mobile navigation on right side of screen */
    $('.sidenav').sidenav({edge: "right"});
    /* Materialize - Display form Select field */
    $('select').formSelect();
});